GLP-1 Companion v5.1.0 — AI Phase 1 CLEAN
===========================================

READ THIS BEFORE UPLOADING TO GITHUB

This package is a CLEAN replacement for the source code in the repository.
It is intentionally based on the current Vite + TypeScript architecture.

EXPECTED ROOT FOLDERS — EXACTLY THESE 4:
  .github/
  public/
  scripts/
  src/

DO NOT KEEP THESE OLD ROOT FOLDERS/FILES:
  assets/
  icons/
  runtime/
  sw.js
  manifest.webmanifest
  .github/workflows/deploy-pages.yml
  dist/
  node_modules/

IMPORTANT:
1. Do NOT delete the GitHub repository itself.
2. Delete the old project files/folders from the main branch first.
3. Extract this ZIP on your computer.
4. Upload ALL CONTENTS of the extracted folder to the ROOT of the GitHub repository.
   Do NOT upload the ZIP itself.
5. Make sure .github/workflows/deploy.yml is present after the upload.
6. Commit directly to the main branch.
7. The push to main should automatically start:
      Actions > Build and deploy GLP-1 Companion
8. GitHub Pages must use "GitHub Actions" as its publishing source.

WHAT THE WORKFLOW CHECKS BEFORE DEPLOYING:
- No legacy assets/, icons/, runtime/, root sw.js, or root manifest remain.
- Source package version is 5.1.0.
- TypeScript validation succeeds.
- Vite build succeeds.
- dist/index.html exists and contains v5.1.0.
- dist/sw.js contains v5.1.0-pwa.
- dist/manifest.webmanifest reports 5.1.0.
- runtime/09-ai-intelligence.js exists in dist.

The build output (dist/) is generated by GitHub Actions. Do not upload dist/ manually.
